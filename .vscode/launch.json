{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run AIR4 (Uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["backend.app.main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"],
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
